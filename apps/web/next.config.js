/** @type {import('next').NextConfig} */
const nextConfig = {
  // NEXT_PUBLIC_API_URL is set in Render dashboard to the full API URL
  // e.g. https://virgil-api-fdlt.onrender.com
  // The browser calls the API directly — no server-side proxy needed.
};

module.exports = nextConfig;
